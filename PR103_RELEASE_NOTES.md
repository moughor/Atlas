# PR103 Release Notes

PR103 adds production-oriented structured logging with JSON and text formats,
correlation IDs, recursive secret redaction, file or stderr output, workspace
lifecycle events, and deterministic field normalization.

Logging remains opt-in, so existing CLI output and library embedding behavior
are unchanged.
