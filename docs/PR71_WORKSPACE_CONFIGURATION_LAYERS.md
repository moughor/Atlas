# PR71 — Workspace Configuration Layers

Adds deterministic `global < workspace < project < CLI` configuration resolution, recursive mapping merge, dotted command-line overrides, file-backed layers, and per-key provenance.
