# PR115 — Review Engine

The Review Engine produces evidence-based Markdown architecture reviews from
ASS facts across architecture, design, layering, dependencies, complexity,
maintainability, naming, and code smells. Categories are normalized
deterministically and reviews can be retained in PR113 memory.

`atlas ai review ROOT [-c CATEGORY]` activates the engine.
