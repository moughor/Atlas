# PR125 Release Notes

PR125 publishes Java, Python, and TypeScript declarations in one deterministic
semantic graph. A built-in non-executing TypeScript/TSX frontend contributes
project-scoped declarations and imports. Existing snapshot fields and CLI
reports remain compatible.
