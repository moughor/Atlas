# PR107 Release Notes

PR107 adds the provider-neutral `moughorai.llm` foundation: immutable message,
request, response, and stream models; a common provider protocol; synchronized
registry; timeout propagation; deterministic retries; and safe streaming.

No provider or network integration is enabled by default.
