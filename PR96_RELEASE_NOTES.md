# PR96 Release Notes

PR96 adds an opt-in, thread-safe performance profiler for workspace and
per-project analyzer timings. Reports aggregate call count, total, minimum,
maximum, and average milliseconds under stable metric names.

Use `atlas profile <workspace>`. Ordinary analysis reports remain unchanged.
