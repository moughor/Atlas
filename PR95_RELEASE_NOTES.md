# PR95 Release Notes

PR95 adds a responsive, self-contained HTML dashboard generated from PR94
analysis history. It summarizes run outcomes, findings, and project activity
without external assets or network access.

Use `atlas dashboard <workspace>` to write `.atlas/dashboard.html`, or select
another output path and history limit with CLI options.
